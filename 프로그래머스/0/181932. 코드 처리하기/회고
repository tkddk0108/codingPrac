for i in code : 이처럼 문자열을 for문에서 사용하면, i 는 문자열의 문자 자체로 인식된다

이리되면 i % 2이런식으로 숫자로 다룰 수 없기 때문에 for 문에서 문자열을 사용하면 안된다

i를 숫자로서 다루고 싶다면

for i in range(len(code))처럼 i는 문자열에서 인덱스를 참조해야 하고, code[i]와 같은 방식으로 문자를 다뤄야 한다!



또한, 이 문제를 실행할때는 잘 되었는데 마지막에 테스트 2개를 통과하지 못했다

그 이유는 지문을 끝까지 안읽었기 때문 ㅎ 

끝까지 잘 읽자!
